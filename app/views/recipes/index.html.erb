<% content_for(:title, t('.title')) %>
<div class="row">
  <div class="col-12">
    <div class="row">
        <% if @recipes.present? %>
        <%= render @recipes %>
        <% else %>
        <p>ありません</p>
        <% end %>
    </div>
  </div>
</div>
